# MongoDB Configuration
MONGO_HOST=mongodb
MONGO_PORT=27017
MONGO_DB_NAME=flask_database

# Application Configuration
APP_HOST=your-domain.com
APP_PORT=5000
APP_PROTOCOL=https
DEBUG=false

# For production, copy this file to .env and update the values
# Example for Ubuntu server deployment:
# APP_HOST=your-server-ip-or-domain
# APP_PROTOCOL=http (or https if you have SSL) 